apiVersion: v1
kind: Service
metadata:
  name: agri-app
  namespace: agri-dev
spec:
  type: {{ .Values.service.type }}
  selector:
    app: agri-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}